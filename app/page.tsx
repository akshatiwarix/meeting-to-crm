export default function Home() {
  return (
    <main className="flex-1 flex items-center justify-center p-8">
      <div className="max-w-md text-center space-y-2">
        <h1 className="font-display italic text-3xl">Meeting to CRM</h1>
        <p className="text-ink-dim">
          Scaffold in progress — the Meeting Library screen lands in a later
          task. See <code>PLAN.md</code> for the implementation task order.
        </p>
      </div>
    </main>
  );
}
